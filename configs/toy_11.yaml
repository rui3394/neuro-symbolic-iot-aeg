name: toy_11_memcpy_truncation_negative
binary: datasets/toy_cgi/build/toy_11
arch: auto

http:
  method: GET
  path: /cgi-bin/ping
  params:
    ip:
      source: true
      max_len: 32

analysis:
  vulnerability_type: truncation_negative_observation
  expected_behavior: truncation_negative_expected
  memory_flow:
    description: memcpy length is too small for the full benign marker before system consumes the buffer
  sinks:
    - system
    - memcpy

mode:
  dry_run: true

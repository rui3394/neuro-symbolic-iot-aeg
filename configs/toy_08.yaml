name: toy_08_snprintf_truncation_negative
binary: datasets/toy_cgi/build/toy_08
arch: auto

http:
  method: GET
  path: /cgi-bin/ping
  params:
    ip:
      source: true
      max_len: 32

analysis:
  vulnerability_type: truncation_negative_observation
  expected_behavior: truncation_negative_expected
  format_flow:
    description: snprintf size truncates the benign marker before system consumes the buffer
  sinks:
    - system
    - snprintf

mode:
  dry_run: true
